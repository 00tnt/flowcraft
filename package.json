{
  "name": "with-tailwind",
  "private": true,
  "scripts": {
    "init-submodules": "./init-submodules.sh",
    "install-fe": "pnpm install",
    "install:fe": "turbo run install-fe",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky",
    "pages:build": "turbo run pages:build",
    "preview": "turbo run preview"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "turbo": "^2.5.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6"
  },
  "packageManager": "pnpm@10.19.0",
  "engines": {
    "node": ">=23"
  }
}
